import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:task_clean_app/app/modules/my_tasks/my_tasks_module.dart';

void main() {
  initModule(MyTasksModule());
  // ListTasksController listtasks;
  //
  setUp(() {
    //     listtasks = MyTasksModule.to.get<ListTasksController>();
  });

  group('ListTasksController Test', () {
    //   test("First Test", () {
    //     expect(listtasks, isInstanceOf<ListTasksController>());
    //   });

    //   test("Set Value", () {
    //     expect(listtasks.value, equals(0));
    //     listtasks.increment();
    //     expect(listtasks.value, equals(1));
    //   });
  });
}
