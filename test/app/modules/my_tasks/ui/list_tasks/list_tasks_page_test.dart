import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('ListTasksPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ListTasksPage(title: 'ListTasks')));
    //  final titleFinder = find.text('ListTasks');
    //  expect(titleFinder, findsOneWidget);
  });
}
