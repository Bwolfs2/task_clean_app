import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('UpdateTaskPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(UpdateTaskPage(title: 'UpdateTask')));
    //  final titleFinder = find.text('UpdateTask');
    //  expect(titleFinder, findsOneWidget);
  });
}
